name: 'Vigilnz Security Scan'
description: 'Run Vigilnz Security scans (SCA, SBOM, SAST) in GitHub Actions'
author: 'Vigilnz'
inputs:
  vigilnzApiKey:
    description: 'Vigilnz API token'
    required: true
  scanTypes:
    description: 'Comma-separated list of scans (sca,sbom,sast)'
    required: true
  # targetFile:
  #   description: 'File or path to scan'
  #   required: false
runs:
  using: 'node16'
  main: 'dist/index.js'

# branding:
#   icon: 'shield'
#   color: 'blue'
